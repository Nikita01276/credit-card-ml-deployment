server {
    listen 80;

    # Передаём все запросы на ML-сервис
    location / {
        proxy_pass http://ml-service:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
